
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: containermanagementobjects.containership.app
spec:
  group: containership.app
  names:
    kind: ContainerManagementObject
    listKind: ContainerManagementObjectList
    plural: containermanagementobjects
    shortNames:
    - cmo
    singular: containermanagementobject
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ContainerManagementObject is the Schema for the containermanagementobjects
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ContainerManagementObjectSpec defines the desired state of
              ContainerManagementObject
            properties:
              images:
                description: Data regarding the images you want pushed with Containership
                items:
                  description: Images contains the metadata for the images to be managed
                    by Containership
                  properties:
                    destinations:
                      description: Destinations are the intended destinations for
                        all imported tags
                      properties:
                        azurecontainerregistries:
                          items:
                            description: AzureContainerRegistries is required information
                              to interact with Azure container registries
                            properties:
                              name:
                                type: string
                              resourceGroup:
                                description: ResourceGroup is optional. Set if you
                                  want to override the global resource group (envvar
                                  AZURE_RESOURCE_GROUP)
                                type: string
                              ring:
                                description: Ring is an optional field that indicates
                                  an image tag should import from a destination with
                                  a subsequent ring value rather than source. Destinations
                                  with higher ring values import tags from destinations
                                  with lower ring values.
                                type: integer
                              subscriptionId:
                                description: SubscriptionID is optional. Set if you
                                  want to override the global subscriptionID (envvar
                                  AZURE_SUBSCRIPTION_ID)
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                        googlecontainerregistries:
                          items:
                            description: GoogleContainerRegistries is required information
                              to interact with Google container registries
                            properties:
                              name:
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                      type: object
                    sourceImage:
                      description: 'SourceImage is the name of the external image
                        to pull. Should not include tags. ex: docker.io/library/busybox'
                      type: string
                    supportedTags:
                      description: SupportedTags are the tags that will be pulled
                        externally and pushed into the destinations
                      items:
                        type: string
                      type: array
                    targetRepository:
                      description: TargetRepository is the name of the image repository
                        that will be pushed to the destination ACR. Only use if you
                        want to rename the image. Do not include tags.
                      type: string
                  required:
                  - destinations
                  - sourceImage
                  - supportedTags
                  type: object
                type: array
              slackWebhookEndpoint:
                description: 'Important: Run "make" to regenerate code after modifying
                  this file SlackWebhookEndpoint is the API endpoint for the Slack
                  channel to which you want alerts sent'
                type: string
            required:
            - images
            type: object
          status:
            description: ContainerManagementObjectStatus defines the observed state
              of ContainerManagementObject
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
